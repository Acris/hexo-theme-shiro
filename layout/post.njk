{% extends "_layout.njk" %}

{% block content %}
{% set article_extra %}
    <!-- Post Navigation: Hexo's page.prev = newer post, page.next = older post -->
    <div class="section-divider flex justify-between items-center text-sm md:text-base">
        {% if page.prev %}
        <a href="{{ html_attr(url_for(page.prev.path)) | safe }}" class="post-nav-link group text-left" title="{{ html_attr(page.prev.title) | safe }}">
             <span aria-hidden="true">&larr;</span>
             <span class="post-nav-title">{{ page.prev.title }}</span>
        </a>
        {% else %}
        <div></div>
        {% endif %}

        {% if page.next %}
        <a href="{{ html_attr(url_for(page.next.path)) | safe }}" class="post-nav-link group text-right" title="{{ html_attr(page.next.title) | safe }}">
            <span class="post-nav-title">{{ page.next.title }}</span>
            <span aria-hidden="true">&rarr;</span>
        </a>
        {% else %}
        <div></div>
        {% endif %}
    </div>
{% endset %}
{% include "_partial/components/article.njk" %}
{% endblock %}
