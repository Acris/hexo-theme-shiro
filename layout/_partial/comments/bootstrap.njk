{# After deferred runtime. Deferred scripts preserve order: bootstrap installs
   runtime.comments before the provider script executes. #}
<script{{ csp_nonce_attr(gates.shiroCspNonce) }}>
    window.__shiro = window.__shiro || {};
    window.__shiro.commentsCss = {{ js_value(versioned_url('css/comments.min.css')) | safe }};
</script>
<script defer{{ csp_nonce_attr(gates.shiroCspNonce) }} src="{{ attr_url(versioned_url('js/comments-bootstrap.min.js')) }}"></script>
