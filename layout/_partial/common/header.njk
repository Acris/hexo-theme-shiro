{% from "_macro/ui.njk" import seal, icon_rss, icon_search, icon_chevron_down, icon_theme_system, icon_theme_light, icon_theme_dark, menu_link with context %}
{% set rss_config = site_config.rss or {} %}
<header class="relative" data-pagefind-ignore>

    <div class="relative flex items-start justify-center">
        <div class="text-center">
            <div class="relative inline-block">
                {% if is_home() %}
                <h1 class="site-title">
                    <a href="{{ url_for('/') }}">{{ escape_html(config.title | default("白夜書簡", true)) }}</a>
                </h1>
                {% else %}
                <p class="site-title">
                    <a href="{{ url_for('/') }}">{{ escape_html(config.title | default("白夜書簡", true)) }}</a>
                </p>
                {% endif %}
                <!-- Seal: Mathematically Centered -->
                {% if site_seal_enabled %}
                <div
                    class="absolute left-full top-1/2 -translate-y-1/2 ml-1 sm:ml-4 origin-left scale-75 sm:scale-110">
                    {{ seal(site_config.seal_text | default("白", true)) }}
                </div>
                {% endif %}
            </div>
            {% if config.subtitle %}
            <p class="site-subtitle">
                {{ escape_html(config.subtitle) }}
            </p>
            {% endif %}
            {% set has_header_actions = rss_config.enabled or dark_mode_toggle or search_enabled %}
            {% if has_header_actions %}
            <div class="mt-3 sm:mt-4 flex items-center justify-center gap-2">
                {% if rss_config.enabled %}
                <a class="focus-elegant header-pill-btn"
                    href="{{ safe_resource_url_for(rss_config.path | default('/atom.xml'), '/atom.xml') }}" target="_blank" rel="noopener noreferrer" aria-label="RSS">
                    {{ icon_rss() }}
                </a>
                {% endif %}
                {% if dark_mode_toggle %}
                <button id="themeToggle" type="button" class="focus-elegant header-pill-btn" aria-label="{{ escape_attr(__('theme.' + dark_mode_default)) }}" data-label-system="{{ escape_attr(__('theme.system')) }}" data-label-light="{{ escape_attr(__('theme.light')) }}" data-label-dark="{{ escape_attr(__('theme.dark')) }}">
                    {{ icon_theme_system() }}
                    {{ icon_theme_light() }}
                    {{ icon_theme_dark() }}
                </button>
                {% endif %}
                {% if search_enabled %}
                {# Header pill; search-bootstrap opens pagefind-modal (dialog id=shiroSearchDialog). #}
                <button id="searchToggle" type="button" class="focus-elegant header-pill-btn" aria-haspopup="dialog" aria-controls="shiroSearchDialog" aria-expanded="false" aria-label="{{ escape_attr(__('search.trigger')) }}">
                    {{ icon_search() }}
                </button>
                {% endif %}
            </div>
            {% endif %}
        </div>
    </div>
</header>

<!-- Navigation -->
{% if menu_items.length %}
<nav class="mt-8 md:mt-10" aria-label="{{ escape_attr(__('nav.menu')) }}" data-pagefind-ignore>
    <!-- Mobile Toggle -->
    <div class="flex items-center justify-center md:hidden">
        <button id="menuBtn" type="button"
            class="focus-elegant inline-flex cursor-pointer items-center gap-2 rounded-full border border-slate-200 bg-paper/95 px-4 py-2 text-sm text-slate-700 shadow-xs"
            aria-expanded="false" aria-controls="mobileMenu">
            <span class="text-base leading-none tracking-widest">{{ escape_html(__('nav.menu')) }}</span>
            {{ icon_chevron_down(id="menuChevron") }}
        </button>
    </div>

    <!-- Desktop Menu -->
    <div class="hidden md:block">
        <ul class="flex flex-wrap items-center justify-center gap-x-6 gap-y-2 text-base text-slate-700 px-4">
            {% for item in menu_items %}
            <li>{{ menu_link(item, "focus-elegant rounded-md px-2 py-1 hover:text-seal transition-colors") }}</li>
            {% if not loop.last %}
            <li class="text-slate-400 select-none" role="separator" aria-hidden="true">/</li>
            {% endif %}
            {% endfor %}
        </ul>
    </div>

    <!-- Mobile Menu Panel: open without JS (progressive enhancement); html.js collapses until toggled. -->
    <div id="mobileMenu" class="menu-panel md:hidden mt-4" data-open="false">
        <ul
            class="mx-auto max-w-sm max-h-menu overflow-y-auto rounded-2xl border border-slate-200 bg-paper/95 shadow-xs">
            {% for item in menu_items %}
            <li>{{ menu_link(item, "focus-elegant block px-6 py-3 text-center text-slate-800 hover:text-seal hover:bg-seal/5 transition-colors") }}</li>
            {% if not loop.last %}
            <li class="h-px bg-slate-200/70" role="separator" aria-hidden="true"></li>
            {% endif %}
            {% endfor %}
        </ul>
    </div>
</nav>
{% endif %}
