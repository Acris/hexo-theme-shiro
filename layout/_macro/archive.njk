{% from "_macro/ui.njk" import icon_nav_arrow with context %}

{# Single archive entry: date + linked title. Shared by both list macros below. #}
{% macro archive_item(post) %}
{% set post_date_attr = date(post.date, 'YYYY-MM-DD') %}
{% set post_date_text = date(post.date, 'YYYY/MM/DD') %}
<article class="group flex items-baseline gap-6">
    <time datetime="{{ post_date_attr }}" class="w-24 shrink-0 text-right text-sm font-medium text-slate-400 oldstyle-nums">
        {{ post_date_text }}
    </time>
    <h3 class="text-base font-normal text-slate-700 transition-colors group-hover:text-seal">
        <a href="{{ url_for(post.path) }}" class="link-underline">{{ post.title }}</a>
    </h3>
</article>
{% endmacro %}

{% macro render_list(posts) %}
    <div class="space-y-16 max-w-2xl mx-auto">
        {% set currentYear = '' %}
        {% set post_list = posts.toArray() %}
        {% for post in post_list %}
            {% set y = date(post.date, 'YYYY') %}
            {% if y != currentYear %}
                {% if currentYear != '' %}
                </div>{# close .space-y-4 #}
                </div>{# close .relative #}
                {% endif %}
                {% set currentYear = y %}
                <div class="relative">{# year group wrapper — closed when year changes or after loop #}
                <h2 class="section-heading">
                    {{ icon_nav_arrow() }}
                    {{ y }}
                </h2>
                <div class="space-y-4">
            {% endif %}
            {{ archive_item(post) }}
        {% endfor %}
        {% if currentYear != '' %}
            </div>{# close .space-y-4 #}
            </div>{# close .relative #}
        {% endif %}
    </div>
{% endmacro %}

{% macro render_simple_list(posts) %}
    <div class="space-y-4">
        {% set post_list = posts.toArray() %}
        {% for post in post_list %}
        {{ archive_item(post) }}
        {% endfor %}
    </div>
{% endmacro %}
